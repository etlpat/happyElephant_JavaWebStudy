<!--
    xml：可扩展标记语言

    xml介绍：
        xml语法和html类似
        html用于显示数据，xml用于传输、存储数据
        html标签不区分大小写，xml标签严格区分大小写
        html标签是定义好的，xml标签是自定义的，并且可以拓展
        html空格是自动过滤的，而xml不会自动过滤空格
        html支持多个根元素，而xml只能存在一个根元素
-->


